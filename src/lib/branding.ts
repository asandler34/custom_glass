/** Brand lockups in `/public/brand/` (from ECG guidelines; replace if assets land in sandman-repo). */
export const brandLogo = {
  /** Warm white + gold — header over hero / navy, charcoal footer */
  onDark: "/brand/ecg-logo-horizontal-light.svg",
  /** Navy + gold — light backgrounds */
  onLight: "/brand/ecg-logo-horizontal.svg",
} as const;
